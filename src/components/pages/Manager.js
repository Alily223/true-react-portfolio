import React from "react";
import { withRouter } from 'react-router-dom';

function Manager() {
    return (
        <div>
            <h1>Manager</h1>
        </div>
    );
}

export default withRouter(Manager);